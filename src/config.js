const config = {
    api: process.env.NEXT_PUBLIC_API_URL || 'https://admin.seekahost.co.uk',
    externalApi: process.env.NEXT_PUBLIC_BACKEND_URL || 'https://admin.seekahost.co.uk'
}

export default config;